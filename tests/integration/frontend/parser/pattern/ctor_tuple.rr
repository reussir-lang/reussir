// RUN: %reussir-parser %s | %FileCheck %s

fn test() {
    match x {
        Foo(1, y) => 1,
        Bar(..) => 2,
        Baz(x, ..) => 3
    }
}

// CHECK: fn test() {
// CHECK:     match x {
// CHECK:         Foo(1, y) => 1
// CHECK:         Bar(..) => 2
// CHECK:         Baz(x, ..) => 3
// CHECK:     }
// CHECK: }
